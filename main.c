#include <stdio.h>

void main() {
    int score;

    // Input the desired score
    printf("Enter the desired NFL score: ");
    scanf("%d", &score);

    printf("All combinations for a score of %d:\n", score);
}
